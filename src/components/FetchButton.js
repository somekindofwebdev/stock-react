export default function FetchButton({ tableId }) {
  function handleClick() {

  }

  return (
    <button
      className="fetch-button"
      onClick={handleClick}
    >
      Find
    </button>
  )
}
